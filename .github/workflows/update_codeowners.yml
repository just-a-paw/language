name: Update CODEOWNERs
on:
  schedule:
    # https://crontab.guru/#5_8_*_*_1,4
    - cron: "5 8 * * 1,4"
  workflow_dispatch:
jobs:
  update:
    runs-on: ubuntu-latest
    if: github.repository == 'OfficialPawBot/language'
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '16'
      - run: npm install
      - run: npm test && npm run update-codeowners
      - run: |
          git config user.name "Paw Bot"
          git config user.email "translating@paw.bot"
      - run: |
          git add .github/CODEOWNERS
          git commit -m 'chore: 🤖 update codeowners' && git push
